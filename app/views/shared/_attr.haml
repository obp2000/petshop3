= form_for attr, :remote => true do |f|
  = f.error_messages
  = render "#{attr.partial_path}/#{attr.class.name.underscore}", :f => f, :attr => attr
  =# render attr, :f => f, :attr => attr 
  = small_submit_button 
  - unless attr.new_record?
    = link_to_delete attr
    = link_to_add_to_item attr