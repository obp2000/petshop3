.ui-widget-content{ :id => objects.dom_id }
  = link_to_close_window objects.first.class
  = will_paginate objects rescue nil
  = render :partial => objects.new_partial, :object => objects.first.class.new1 rescue nil
  = render :partial => objects.edit_partial, :collection => objects unless objects.first.new_record? rescue nil  
  = draggable_element objects.dom_id
  