= link_to_show order_item.item
- [ "size", "colour" ].each do |attr|
  = render "shared/#{attr}", :"#{attr}" => order_item.send( attr ) rescue nil  
= order_item.amount
= SHT + ","
= roubles order_item.order_item_sum
= RUB
%br