%h2= index_page_title_for ForumPost
%fieldset#forum.ui-widget-content
  = link_to_new ForumPost
  = ForumPost.no_forum_posts_text unless @objects.first.id
  %div{ :id => @objects.first.class.name.tableize }= render @objects rescue nil  
  = will_paginate @objects rescue nil
#post
#post_new
 