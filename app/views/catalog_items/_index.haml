%div{ :id => @objects.first.class.name.tableize }
  %h2= index_page_title_for @objects.first.class
  = render :partial => "catalog_items/catalog_item", :collection => @objects
  = will_paginate @objects
#details