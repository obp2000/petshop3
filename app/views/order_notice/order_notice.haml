= "#{t(:dear)} #{@order.ship_to_first_name} !"
= @order.human_attribute_name( :order_accepted )
= @order.human_attribute_name( :id ) + ": " + @order.id.to_s
= @order.human_attribute_name( :order_notice_header )
= @order.human_attribute_name( :order_info )
- @order.order_items.each do |order_item|
  = order_notice( order_item )
= @order.human_attribute_name( :order_total_sum ) + ": " +  number_to_currency( @order.total )
= @order.human_attribute_name( :thank_you_for_order )
= @order.human_attribute_name( :order_notice_sign ).html_safe

