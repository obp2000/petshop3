%span{ :id => dom_id( attr ) }
  = render :partial => "#{SharedPath}/#{attr.underscore}", :object => attr unless attr.blank?  
  = link_to_remove_from_item attr
  - [ attr.id, 0 ].each do |id|
    = hidden_field_tag attr.hidden_field_name, id   